---
phase: 05-async-enricher-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/enrichment_service.py
  - tests/test_enrichment_service.py
autonomous: true

must_haves:
  truths:
    - "When primary API returns no price data, YFinance fallback is attempted"
    - "YFinance calls do not block the async event loop"
    - "Enriched clusters show used_yfinance_fallback=true when fallback provided data"
  artifacts:
    - path: "src/services/enrichment_service.py"
      provides: "YFinance async fallback methods"
      contains: "_fetch_price_yfinance_async"
    - path: "tests/test_enrichment_service.py"
      provides: "Fallback behavior tests"
      contains: "test_yfinance_fallback"
  key_links:
    - from: "src/services/enrichment_service.py"
      to: "yfinance"
      via: "asyncio.to_thread wrapper"
      pattern: "asyncio\\.to_thread.*_fetch_price_yfinance"
    - from: "AsyncEnricher.get_price_history"
      to: "_fetch_price_yfinance_async"
      via: "fallback on empty API response"
      pattern: "await self\\._fetch_price_yfinance_async"
---

<objective>
Add YFinance fallback to AsyncEnricher for price fetching parity with sync script.

Purpose: When Financial Datasets API returns no data (400, empty response), fall back to free YFinance data to maximize enrichment success rate. This matches the sync script behavior at lines 284-321.

Output: Modified AsyncEnricher with yfinance fallback capability, tests verifying fallback triggers correctly.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-async-enricher-parity/05-RESEARCH.md

# Reference implementation
@scripts/enrich_clusters_with_price.py (lines 284-321 for YFinance sync)

# Target file
@src/services/enrichment_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add async YFinance fallback methods to AsyncEnricher</name>
  <files>src/services/enrichment_service.py</files>
  <action>
Add two methods to AsyncEnricher class:

1. `_fetch_price_yfinance_sync(self, ticker: str, target_date: date) -> float | None`:
   - Copy logic from sync script lines 284-321
   - Use `yf.Ticker(ticker)` - create new instance per call (thread safety)
   - Request 7-day window: start = target_date - 7 days, end = target_date + 1 day
   - Call `stock.history(start=..., end=..., auto_adjust=True, repair=True)`
   - Convert index to dates, find closest date <= target_date
   - Return float price or None on any exception
   - Handle exceptions: ValueError, KeyError, TypeError, AttributeError

2. `_fetch_price_yfinance_async(self, ticker: str, target_date: date) -> float | None`:
   - Wrap sync method with `await asyncio.to_thread(self._fetch_price_yfinance_sync, ticker, target_date)`
   - Catch Exception at outer level, log error, return None

3. Modify `get_price_history()` method:
   - After checking cache and API, if `api_prices` is empty, try yfinance fallback
   - Call `fallback_price = await self._fetch_price_yfinance_async(ticker, start.date())`
   - If fallback_price is not None, create synthetic history: `[{"date": start, "close": fallback_price}]`
   - Track fallback usage in return value (add optional return flag or attribute)

4. Modify `enrich_cluster()` method:
   - After getting price history, check if fallback was used
   - Add `used_yfinance_fallback` boolean field to enriched cluster output

Add import at top: `import yfinance as yf` and `from datetime import date`
  </action>
  <verify>
Run: `python -c "from src.services.enrichment_service import AsyncEnricher; print('Import OK')"`
Grep: `grep -n "_fetch_price_yfinance" src/services/enrichment_service.py` shows both methods
Grep: `grep -n "asyncio.to_thread" src/services/enrichment_service.py` confirms async wrapper
  </verify>
  <done>
AsyncEnricher has _fetch_price_yfinance_sync, _fetch_price_yfinance_async methods.
get_price_history() tries yfinance fallback when API returns empty.
enrich_cluster() adds used_yfinance_fallback field to output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for YFinance fallback behavior</name>
  <files>tests/test_enrichment_service.py</files>
  <action>
Add test cases for YFinance fallback in existing test file:

1. `test_yfinance_fallback_on_empty_api_response`:
   - Mock AsyncHTTPClient.get to return empty prices list
   - Mock _fetch_price_yfinance_sync to return 100.0
   - Call enricher.get_price_history()
   - Assert result contains fallback price
   - Assert yfinance mock was called

2. `test_yfinance_fallback_not_called_when_api_succeeds`:
   - Mock AsyncHTTPClient.get to return valid prices
   - Mock _fetch_price_yfinance_sync
   - Call enricher.get_price_history()
   - Assert yfinance mock was NOT called

3. `test_yfinance_async_wrapper_uses_to_thread`:
   - Verify asyncio.to_thread is used (mock and check)
   - Ensures non-blocking behavior

4. `test_enrich_cluster_tracks_fallback_usage`:
   - Mock API to return empty, yfinance to return price
   - Call enrich_cluster()
   - Assert output has `used_yfinance_fallback: True`

Use pytest and unittest.mock. Follow existing test patterns in the file.
  </action>
  <verify>
Run: `pytest tests/test_enrichment_service.py -v -k yfinance`
All yfinance fallback tests pass.
  </verify>
  <done>
4 tests covering: fallback triggers on empty API, fallback skipped when API succeeds, async wrapper uses to_thread, cluster output tracks fallback usage.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_enrichment_service.py -v` - all tests pass
2. `grep -n "yfinance" src/services/enrichment_service.py` - shows yfinance integration
3. `python -c "import asyncio; from src.services.enrichment_service import AsyncEnricher; print('Async import OK')"` - no blocking imports
</verification>

<success_criteria>
- AsyncEnricher has yfinance fallback for price fetching
- Fallback only triggers when primary API returns no data
- asyncio.to_thread wraps blocking yfinance calls
- Enriched clusters track whether fallback was used
- All tests pass including new fallback tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-async-enricher-parity/05-01-SUMMARY.md`
</output>
