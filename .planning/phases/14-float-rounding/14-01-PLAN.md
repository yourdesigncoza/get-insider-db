---
phase: 14-float-rounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scan_clusters.py
  - tests/test_float_rounding.py
autonomous: true

must_haves:
  truths:
    - "All 5 float fields (cluster_score, avg_percent_change, avg_days_to_file, fund_ratio, avg_sale_to_purchase_ratio) are rounded to 2 decimal places in JSON export"
    - "Rounding only affects the export copy (out_df), not internal calculation DataFrame (df)"
    - "Integer magnitude fields (total_shares, total_value) are NOT rounded"
  artifacts:
    - path: "scripts/scan_clusters.py"
      provides: "Extended rounding loop covering all 5 float fields"
      contains: "FLOAT_FIELDS_TO_ROUND"
    - path: "tests/test_float_rounding.py"
      provides: "Unit tests verifying rounding behavior"
      min_lines: 30
  key_links:
    - from: "scripts/scan_clusters.py"
      to: "out_df (export copy)"
      via: "FLOAT_FIELDS_TO_ROUND list iterated with .round(2)"
      pattern: "FLOAT_FIELDS_TO_ROUND.*round\\(2\\)"
---

<objective>
Extend the existing partial float rounding in scan_clusters.py to cover all 5 floating-point fields before JSON export. Currently only cluster_score and avg_percent_change are rounded; avg_days_to_file, fund_ratio, and avg_sale_to_purchase_ratio still serialize with full precision (e.g., 0.8333333333333334).

Purpose: Requirement OUT-02 -- readable, consistent JSON exports with 2-decimal float precision
Output: Modified scan_clusters.py with complete rounding, test file validating behavior
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-float-rounding/14-RESEARCH.md
@scripts/scan_clusters.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend float rounding to all 5 fields in scan_clusters.py</name>
  <files>scripts/scan_clusters.py</files>
  <action>
At line 332 in scripts/scan_clusters.py, replace the existing 2-field rounding loop:

```python
for col in ("cluster_score", "avg_percent_change"):
    if col in out_df.columns:
        out_df[col] = out_df[col].round(2)
```

With an explicit named list covering all 5 float fields:

```python
# Round all floating-point fields to 2 decimals for export readability (OUT-02)
FLOAT_FIELDS_TO_ROUND = [
    "cluster_score",
    "avg_percent_change",
    "avg_days_to_file",
    "fund_ratio",
    "avg_sale_to_purchase_ratio",
]
for col in FLOAT_FIELDS_TO_ROUND:
    if col in out_df.columns:
        out_df[col] = out_df[col].round(2)
```

Define FLOAT_FIELDS_TO_ROUND at module level (top of file, after imports) so it can be imported by tests. The loop stays in its current position (after out_df = df.copy() and column drops, before metadata construction).

Do NOT round total_shares or total_value -- these are integer-magnitude fields that use .0f formatters.
Do NOT move the rounding before df.copy() -- it must only affect the export copy, not internal calculations.
  </action>
  <verify>
Run: python -c "from scripts.scan_clusters import FLOAT_FIELDS_TO_ROUND; print(FLOAT_FIELDS_TO_ROUND); assert len(FLOAT_FIELDS_TO_ROUND) == 5"

If import fails due to script structure, verify with grep:
grep -c "FLOAT_FIELDS_TO_ROUND" scripts/scan_clusters.py  (should be >= 2: definition + usage)
grep "avg_days_to_file" scripts/scan_clusters.py  (should appear in the list)
grep "fund_ratio" scripts/scan_clusters.py  (should appear in the list)
grep "avg_sale_to_purchase_ratio" scripts/scan_clusters.py  (should appear in the list)
  </verify>
  <done>
All 5 float fields listed in FLOAT_FIELDS_TO_ROUND. The rounding loop iterates over all 5 fields. Rounding operates only on out_df (the export copy created at line 325). total_shares and total_value are NOT in the list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for float rounding behavior</name>
  <files>tests/test_float_rounding.py</files>
  <action>
Create tests/test_float_rounding.py with the following test cases using pandas DataFrames directly (no database needed):

1. **test_all_float_fields_rounded** - Create a DataFrame with all 5 float fields set to values with many decimal places (e.g., avg_days_to_file=0.8333333333, fund_ratio=0.166666667, avg_sale_to_purchase_ratio=0.142857143, cluster_score=72.456789, avg_percent_change=3.141592). Apply the same rounding logic used in scan_clusters.py. Assert each field has at most 2 decimal places after rounding.

2. **test_magnitude_fields_not_rounded** - Create a DataFrame with total_shares=1234567.89 and total_value=9876543.21. Verify these fields are NOT in FLOAT_FIELDS_TO_ROUND (import the constant from scan_clusters or hardcode and assert against it).

3. **test_rounding_preserves_nan** - Create a DataFrame with NaN values in float fields. Apply .round(2). Assert NaN values remain NaN (not converted to 0 or causing errors).

4. **test_rounding_does_not_mutate_original** - Create original DataFrame, make a copy, apply rounding to copy. Assert original values unchanged (proves out_df pattern is safe).

5. **test_missing_columns_handled** - Create a DataFrame missing some float fields (e.g., only has cluster_score). Apply rounding loop with `if col in df.columns` guard. Assert no KeyError, existing columns still rounded.

Use pytest. Import pandas. Follow existing test patterns in tests/ directory (simple, focused, no fixtures needed).
  </action>
  <verify>
Run: pytest tests/test_float_rounding.py -v
All 5 tests should pass.
  </verify>
  <done>
5 test cases pass covering: all fields rounded, magnitude fields excluded, NaN handling, immutability of original DataFrame, graceful handling of missing columns.
  </done>
</task>

</tasks>

<verification>
1. grep "FLOAT_FIELDS_TO_ROUND" scripts/scan_clusters.py confirms constant defined and used
2. pytest tests/test_float_rounding.py -v shows all 5 tests passing
3. pytest tests/ shows no regressions in existing test suite
4. Manual spot check (optional): python scripts/scan_clusters.py --limit 2 --print and inspect JSON output for rounded values
</verification>

<success_criteria>
- FLOAT_FIELDS_TO_ROUND contains exactly 5 fields: cluster_score, avg_percent_change, avg_days_to_file, fund_ratio, avg_sale_to_purchase_ratio
- Rounding loop applies .round(2) to all 5 fields on out_df only
- total_shares and total_value are NOT rounded
- 5 unit tests pass validating rounding behavior
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/14-float-rounding/14-01-SUMMARY.md`
</output>
