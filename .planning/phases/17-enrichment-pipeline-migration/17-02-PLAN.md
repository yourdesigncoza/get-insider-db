---
phase: 17-enrichment-pipeline-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/enrichment_service.py
  - scripts/enrich_clusters_async.py
autonomous: true

must_haves:
  truths:
    - "AsyncEnricher uses issuer_cik for all DB cache queries (SELECT, INSERT, ON CONFLICT)"
    - "AsyncEnricher resolves ticker from CikTickerMapper for API calls and YFinance fallback"
    - "Async CLI script excludes clusters with missing issuer_cik from enrichment"
    - "Async CLI script excludes clusters with valid CIK but no ticker mapping"
    - "Async CLI completion prints resolution statistics: resolved, missing CIK, unmapped"
    - "Async progress logs display CIK (TICKER) format during enrichment"
  artifacts:
    - path: "src/services/enrichment_service.py"
      provides: "CIK-based AsyncEnricher with mapper integration"
      contains: "issuer_cik"
    - path: "scripts/enrich_clusters_async.py"
      provides: "CIK-aware async CLI with pre-validation and resolution stats"
      contains: "missing_cik"
  key_links:
    - from: "src/services/enrichment_service.py"
      to: "src/services/cik_ticker_mapping.py"
      via: "get_mapper() in AsyncEnricher.__init__()"
      pattern: "self._mapper = get_mapper"
    - from: "src/services/enrichment_service.py"
      to: "market_prices table"
      via: "SQL WHERE issuer_cik = :issuer_cik"
      pattern: "WHERE issuer_cik = :issuer_cik"
    - from: "scripts/enrich_clusters_async.py"
      to: "src/services/cik_ticker_mapping.py"
      via: "get_mapper() for pre-validation"
      pattern: "from src.services.cik_ticker_mapping import get_mapper"
---

<objective>
Migrate async enrichment service and CLI script to CIK-based primary keys with pre-validation and resolution statistics.

Purpose: Complete CIK migration for the async enrichment path (both the `AsyncEnricher` service class and the `enrich_clusters_async.py` CLI) so all DB cache queries use issuer_cik, tickers are resolved via CikTickerMapper, and clusters without valid CIK are excluded with statistics reported.

Output: Updated `src/services/enrichment_service.py` and `scripts/enrich_clusters_async.py` with CIK-first approach.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-schema-re-keying/16-01-SUMMARY.md
@.planning/phases/17-enrichment-pipeline-migration/17-RESEARCH.md
@src/services/enrichment_service.py
@scripts/enrich_clusters_async.py
@src/services/cik_ticker_mapping.py
@schema.sql (lines 499-622 for market_prices and market_fundamentals DDL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AsyncEnricher to CIK-based cache queries</name>
  <files>src/services/enrichment_service.py</files>
  <action>
**1. Import CikTickerMapper:**
Add `from src.services.cik_ticker_mapping import get_mapper` to imports.

**2. Inject mapper into `AsyncEnricher.__init__()`:**
Add `self._mapper = get_mapper()` after existing initialization.

**3. Update `_check_price_cache()` signature and SQL:**
- Change parameter from `ticker: str` to `issuer_cik: str`.
- SQL: `WHERE issuer_cik = :issuer_cik` (was `WHERE ticker = :ticker`).
- Bind param: `"issuer_cik": issuer_cik`.

**4. Update `_save_prices_to_cache()` signature and SQL:**
- Change signature to `(self, issuer_cik: str, ticker: str, prices: list[dict])`.
- SQL: `INSERT INTO market_prices (issuer_cik, ticker, price_date, close_price) VALUES (:issuer_cik, :ticker, :date, :price)`.
- ON CONFLICT: `ON CONFLICT (issuer_cik, price_date) DO NOTHING`.
- Bind params: `"issuer_cik": issuer_cik, "ticker": ticker`.

**5. Update `_check_fundamentals_cache()` signature and SQL:**
- Change parameter from `ticker: str` to `issuer_cik: str`.
- SQL: `WHERE issuer_cik = :issuer_cik` (was `WHERE ticker = :ticker`).
- Bind param: `"issuer_cik": issuer_cik`.

**6. Update `_save_fundamentals_to_cache()` signature and SQL:**
- Change signature to `(self, issuer_cik: str, ticker: str, data: dict)`.
- SQL: `INSERT INTO market_fundamentals (issuer_cik, ticker, date, ...) VALUES (:issuer_cik, :ticker, :date, ...)`.
- ON CONFLICT: `ON CONFLICT (issuer_cik, date) DO NOTHING`.
- Bind params include both `issuer_cik` and `ticker`.

**7. Update `get_price_history()` for CIK-first approach:**
- Change signature to `(self, issuer_cik: str, start, end)` (remove ticker param).
- Resolve ticker internally: `ticker = self._mapper.get_ticker(issuer_cik)`. If not ticker, raise `ValueError(f"No ticker mapping for CIK: {issuer_cik}")`.
- Pass `issuer_cik` to `_check_price_cache()`.
- API call `_fetch_prices_from_api()` still uses `ticker`.
- Pass `issuer_cik` and `ticker` to `_save_prices_to_cache()`.
- YFinance fallback still uses `ticker`.
- Return type unchanged: `tuple[list[dict], bool]`.

**8. Update `get_fundamentals()` for CIK-first approach:**
- Change signature to `(self, issuer_cik: str, target_date)` (remove ticker param).
- Resolve ticker internally: `ticker = self._mapper.get_ticker(issuer_cik)`. If not ticker, raise `ValueError(f"No ticker mapping for CIK: {issuer_cik}")`.
- Pass `issuer_cik` to `_check_fundamentals_cache()`.
- API call `_fetch_fundamentals_from_api()` still uses `ticker`.
- Pass `issuer_cik` and `ticker` to `_save_fundamentals_to_cache()`.

**9. Update `enrich_cluster()` to use CIK-first approach:**
- Extract `issuer_cik = cluster.get("issuer_cik")`.
- Early return (return unmodified cluster) if `not issuer_cik` (pre-validation happens in CLI script, but defense-in-depth).
- Resolve ticker from mapper: `ticker = self._mapper.get_ticker(issuer_cik)`. If not ticker, return cluster with `enrichment_status = "unmapped_cik"`.
- Replace `ticker = cluster.get("ticker")` with mapper-resolved ticker.
- Pass `issuer_cik` to `get_price_history()` and `get_fundamentals()` (instead of ticker).
- Keep `window_end_str` and `entry_date` logic unchanged.

**10. Update `enrich_batch()` error logging:**
- Include `issuer_cik` in the EnrichmentError context dict.
  </action>
  <verify>
Run: `python -c "from src.services.enrichment_service import AsyncEnricher; print('import ok')"` to verify no syntax errors.

Grep for old patterns that should NOT exist:
- `grep -n "WHERE ticker = :ticker" src/services/enrichment_service.py` should return 0 results.
- `grep -n "ON CONFLICT (ticker" src/services/enrichment_service.py` should return 0 results.

Grep for new patterns that MUST exist:
- `grep -c "issuer_cik" src/services/enrichment_service.py` should return >10 matches.
- `grep -c "get_mapper\|self._mapper" src/services/enrichment_service.py` should return >=3 matches.
  </verify>
  <done>
- All 4 cache SQL queries use `WHERE issuer_cik = :issuer_cik`
- All 2 ON CONFLICT clauses use `(issuer_cik, ...)`
- INSERT statements include both issuer_cik and ticker columns
- CikTickerMapper injected in `__init__()` and used in `get_price_history()`, `get_fundamentals()`, `enrich_cluster()`
- `enrich_cluster()` extracts CIK from cluster dict, resolves ticker via mapper
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CIK pre-validation and resolution stats to async CLI</name>
  <files>scripts/enrich_clusters_async.py</files>
  <action>
**1. Import CikTickerMapper:**
Add `from src.services.cik_ticker_mapping import get_mapper` to imports.

**2. Add CIK resolution fields to `EnrichmentStats`:**
Add fields: `missing_cik: int = 0`, `unmapped_cik: int = 0`, `resolved: int = 0`.
Update `report()` to log resolution statistics: `f"CIK resolution: {self.resolved}/{self.total_clusters + self.missing_cik + self.unmapped_cik} resolved, {self.missing_cik} missing CIK, {self.unmapped_cik} no ticker mapping"`.

**3. Update `enrich_streaming()` with pre-validation:**
- Initialize mapper at start: `mapper = get_mapper()`.
- Before enrichment loop, add CIK validation for each cluster in the batch:
  - If `not cluster.get("issuer_cik")`: increment `stats.missing_cik`, skip cluster (do not write to output).
  - If `not mapper.get_ticker(cluster["issuer_cik"])`: increment `stats.unmapped_cik`, skip cluster.
  - Otherwise: proceed with enrichment, increment `stats.resolved` after success.
- Update progress line: `print(f"  [{processed}/{total_clusters}] Enriched {cluster.get('issuer_cik', '?')} ({ticker})")`.

**4. Update `enrich_small_file()` with pre-validation:**
- Initialize mapper at start: `mapper = get_mapper()`.
- Before calling `enricher.enrich_cluster(cluster)`, validate:
  - If `not cluster.get("issuer_cik")`: increment `stats.missing_cik`, skip (append original to enriched_rows or skip entirely -- per requirement, EXCLUDE from output).
  - If `not mapper.get_ticker(cluster["issuer_cik"])`: increment `stats.unmapped_cik`, skip.
  - Otherwise: proceed, increment `stats.resolved`.
- Update progress line to CIK (TICKER) format.
- Note: Excluded clusters should NOT be appended to enriched_rows (they are excluded from output entirely per ENRICH-02).

**5. Update progress display format in both functions:**
- In `enrich_streaming()`: `print(f"  [{processed}/{total_clusters}] Enriched {issuer_cik} ({ticker})")`.
- In `enrich_small_file()`: `print(f"  [{i + 1}/{total}] Enriched {issuer_cik} ({ticker})")`.
- For excluded clusters, log: `logger.info(f"  [{i + 1}/{total}] Skipped {issuer_cik or 'NO_CIK'} (excluded)")`.

**6. Update `process_file()` completion output:**
- After `stats.report()`, the resolution statistics are already included from the updated report method.
  </action>
  <verify>
Run: `python -c "from scripts.enrich_clusters_async import EnrichmentStats; print('import ok')"` to verify no syntax errors.

Grep for new patterns that MUST exist:
- `grep -c "missing_cik" scripts/enrich_clusters_async.py` should return >=3 matches.
- `grep -c "unmapped_cik" scripts/enrich_clusters_async.py` should return >=3 matches.
- `grep -c "get_mapper" scripts/enrich_clusters_async.py` should return >=1 match.
- `grep -c "issuer_cik" scripts/enrich_clusters_async.py` should return >=5 matches.

Run async enrichment-related tests: `pytest tests/ -k "async" -x --tb=short`.
  </verify>
  <done>
- Pre-validation excludes clusters without issuer_cik from output
- Pre-validation excludes clusters with unmapped CIK from output
- EnrichmentStats tracks missing_cik, unmapped_cik, resolved counts
- report() prints resolution statistics at completion
- Progress logs show "CIK (TICKER)" format
- Excluded clusters are not appended to output
  </done>
</task>

</tasks>

<verification>
1. No remaining `WHERE ticker = :ticker` in `src/services/enrichment_service.py`
2. No remaining `ON CONFLICT (ticker,` in `src/services/enrichment_service.py`
3. AsyncEnricher has `self._mapper` initialized in `__init__()`
4. `enrich_cluster()` uses `cluster.get("issuer_cik")` not `cluster.get("ticker")` for identity
5. Async CLI validates CIK before enrichment in both streaming and memory modes
6. Stats report includes resolution counts
7. Progress logs show "CIK (TICKER)" format
8. `python -c "from src.services.enrichment_service import AsyncEnricher"` succeeds
9. `python -c "from scripts.enrich_clusters_async import EnrichmentStats"` succeeds
</verification>

<success_criteria>
- `grep "WHERE ticker = :ticker" src/services/enrichment_service.py` returns no matches
- `grep "ON CONFLICT (ticker" src/services/enrichment_service.py` returns no matches
- `grep "issuer_cik" src/services/enrichment_service.py` returns 10+ matches
- `grep "self._mapper" src/services/enrichment_service.py` returns 3+ matches
- `grep "missing_cik" scripts/enrich_clusters_async.py` returns 3+ matches
- Both scripts import cleanly without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-enrichment-pipeline-migration/17-02-SUMMARY.md`
</output>
