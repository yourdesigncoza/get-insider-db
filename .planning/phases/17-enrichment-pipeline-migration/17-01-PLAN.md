---
phase: 17-enrichment-pipeline-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enrich_clusters_with_price.py
autonomous: true

must_haves:
  truths:
    - "Sync enrichment script uses issuer_cik for all DB cache queries (SELECT, INSERT, ON CONFLICT)"
    - "Ticker resolved from CikTickerMapper for Financial Datasets API calls only"
    - "Clusters with missing issuer_cik excluded from enrichment output entirely"
    - "Clusters with valid CIK but no ticker mapping excluded from enrichment output entirely"
    - "Completion prints resolution statistics: resolved count, missing CIK, unmapped CIK"
    - "Progress logs display CIK (TICKER) format during enrichment"
  artifacts:
    - path: "scripts/enrich_clusters_with_price.py"
      provides: "CIK-based sync enrichment with pre-validation and resolution stats"
      contains: "issuer_cik"
  key_links:
    - from: "scripts/enrich_clusters_with_price.py"
      to: "src/services/cik_ticker_mapping.py"
      via: "get_mapper() singleton import"
      pattern: "from src.services.cik_ticker_mapping import get_mapper"
    - from: "scripts/enrich_clusters_with_price.py"
      to: "market_prices table"
      via: "SQL WHERE issuer_cik = :issuer_cik"
      pattern: "WHERE issuer_cik = :issuer_cik"
    - from: "scripts/enrich_clusters_with_price.py"
      to: "market_fundamentals table"
      via: "SQL WHERE issuer_cik = :issuer_cik"
      pattern: "WHERE issuer_cik = :issuer_cik"
---

<objective>
Migrate sync enrichment script to CIK-based primary keys with pre-validation and resolution statistics.

Purpose: Complete CIK migration for the synchronous enrichment path so all DB cache queries use issuer_cik (matching Phase 16 schema) while resolving tickers from CikTickerMapper for external API calls.

Output: Updated `scripts/enrich_clusters_with_price.py` with CIK-first cache lookups, strict exclusion, and resolution statistics.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-schema-re-keying/16-01-SUMMARY.md
@.planning/phases/17-enrichment-pipeline-migration/17-RESEARCH.md
@scripts/enrich_clusters_with_price.py
@src/services/cik_ticker_mapping.py
@schema.sql (lines 499-622 for market_prices and market_fundamentals DDL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sync enrichment to CIK-based cache + pre-validation</name>
  <files>scripts/enrich_clusters_with_price.py</files>
  <action>
**1. Import CikTickerMapper:**
Add `from src.services.cik_ticker_mapping import get_mapper` to imports.

**2. Update `_fetch_prices_from_db()` signature and SQL:**
- Change parameter from `ticker: str` to `issuer_cik: str`.
- SQL: `WHERE issuer_cik = :issuer_cik` (was `WHERE ticker = :ticker`).
- Bind param: `"issuer_cik": issuer_cik`.
- Update debug log messages to include CIK.

**3. Update `_save_prices_to_db()` signature and SQL:**
- Change signature to `(issuer_cik: str, ticker: str, prices: ...)`.
- SQL: `INSERT INTO market_prices (issuer_cik, ticker, price_date, close_price) VALUES (:issuer_cik, :ticker, :date, :price)`.
- ON CONFLICT: `ON CONFLICT (issuer_cik, price_date) DO NOTHING`.
- Bind params: `"issuer_cik": issuer_cik, "ticker": ticker`.

**4. Update `_get_price_history()` signature and logic:**
- Change signature to `(issuer_cik: str, ticker: str, start_date, end_date)`.
- Pass `issuer_cik` to `_fetch_prices_from_db()`.
- API call still uses `ticker` in params dict (for Financial Datasets API).
- Pass both `issuer_cik` and `ticker` to `_save_prices_to_db()`.
- Update all log messages to use `f"{issuer_cik} ({ticker})"` format.
- Note: `@lru_cache` must be removed or updated since signature changes to include both issuer_cik and ticker (consider removing it as it causes issues with mutable default args; the DB cache already serves this purpose).

**5. Update `_fetch_fundamentals_from_db()` signature and SQL:**
- Change parameter from `ticker: str` to `issuer_cik: str`.
- SQL: `WHERE issuer_cik = :issuer_cik` (was `WHERE ticker = :ticker`).
- Bind param: `"issuer_cik": issuer_cik`.

**6. Update `_save_fundamentals_to_db()` signature and SQL:**
- Change signature to `(issuer_cik: str, ticker: str, data: ...)`.
- SQL: `INSERT INTO market_fundamentals (issuer_cik, ticker, date, ...) VALUES (:issuer_cik, :ticker, :date, ...)`.
- ON CONFLICT: `ON CONFLICT (issuer_cik, date) DO NOTHING`.
- Bind params include both `issuer_cik` and `ticker`.

**7. Update `_get_fundamental_at_date()` signature:**
- Change signature to `(issuer_cik: str, ticker: str, target_date, price_at_date)`.
- Pass `issuer_cik` to `_fetch_fundamentals_from_db()`.
- API call still uses `ticker` in params.
- Pass both `issuer_cik` and `ticker` to `_save_fundamentals_to_db()`.
- Update log messages to `f"{issuer_cik} ({ticker})"`.

**8. Add CIK resolution stats to `EnrichmentStats`:**
Add fields: `missing_cik: int = 0`, `unmapped_cik: int = 0`, `resolved: int = 0`.
Update `report()` method to print: `f"CIK resolution: {self.resolved}/{self.total_clusters} resolved, {self.missing_cik} missing CIK, {self.unmapped_cik} no ticker mapping"`.

**9. Update `enrich_row()` for CIK-first approach:**
- Initialize mapper at module level: `mapper = None` (lazy init).
- At top of `enrich_row()`, lazy-init mapper: `global mapper; if mapper is None: mapper = get_mapper()`.
- Extract `issuer_cik = row.get("issuer_cik")`.
- Pre-validate: if `not issuer_cik`, log warning with structlog, increment `stats.missing_cik`, return `row` (excluded).
- Resolve ticker: `ticker = mapper.get_ticker(issuer_cik)`. If `not ticker`, log warning, increment `stats.unmapped_cik`, return `row` (excluded).
- Increment `stats.resolved`.
- Replace all `ticker` references in this function: use resolved `ticker` for the variable (the cluster's `row.get("ticker")` is ignored in favor of mapper-resolved ticker).
- Pass `issuer_cik` and `ticker` to `_get_price_history()` and `_get_fundamental_at_date()`.

**10. Update `process_file()` progress logs:**
- Change `logger.info(f"  [{i+1}/{total}] Enriching {ticker}...")` to use CIK (TICKER) format.
- Extract `issuer_cik` from row and use `f"{issuer_cik} ({ticker})"` or `f"{issuer_cik} (unmapped)"` if no ticker.

**11. Update `_fetch_price_yfinance()` log messages:**
- Include CIK where available (this function only takes ticker, so no change to signature, but caller context already has CIK for logs).
  </action>
  <verify>
Run: `python -c "from scripts.enrich_clusters_with_price import enrich_row; print('import ok')"` to verify no syntax errors.

Grep for old patterns that should NOT exist:
- `grep -n "WHERE ticker = :ticker" scripts/enrich_clusters_with_price.py` should return 0 results.
- `grep -n "ON CONFLICT (ticker" scripts/enrich_clusters_with_price.py` should return 0 results.

Grep for new patterns that MUST exist:
- `grep -c "issuer_cik" scripts/enrich_clusters_with_price.py` should return >10 matches.
- `grep -c "get_mapper" scripts/enrich_clusters_with_price.py` should return >=1 match.
- `grep -c "missing_cik" scripts/enrich_clusters_with_price.py` should return >=2 matches.
- `grep -c "unmapped_cik" scripts/enrich_clusters_with_price.py` should return >=2 matches.

Run enrichment-related tests: `pytest tests/ -k "enrich" -x --tb=short` (some may still fail if test fixtures need updating, but import errors and SQL syntax should be clean).
  </verify>
  <done>
- All 4 cache SQL queries use `WHERE issuer_cik = :issuer_cik` (not ticker)
- All 2 ON CONFLICT clauses use `(issuer_cik, ...)` (not `(ticker, ...)`)
- INSERT statements include both issuer_cik and ticker columns
- CikTickerMapper imported and used to resolve ticker from CIK
- Pre-validation excludes clusters with missing CIK or unmapped CIK
- EnrichmentStats reports CIK resolution counts
- Progress logs show "CIK (TICKER)" format
  </done>
</task>

</tasks>

<verification>
1. No remaining `WHERE ticker = :ticker` in `scripts/enrich_clusters_with_price.py`
2. No remaining `ON CONFLICT (ticker,` in `scripts/enrich_clusters_with_price.py`
3. All INSERT statements include `issuer_cik` column
4. CikTickerMapper singleton used (not per-cluster DB queries)
5. Clusters without CIK return early (excluded from output)
6. Clusters with CIK but no mapping return early (excluded from output)
7. Stats report shows resolution counts at completion
8. Progress logs show CIK (TICKER) format
</verification>

<success_criteria>
- `grep "WHERE ticker = :ticker" scripts/enrich_clusters_with_price.py` returns no matches
- `grep "ON CONFLICT (ticker" scripts/enrich_clusters_with_price.py` returns no matches
- `grep "issuer_cik" scripts/enrich_clusters_with_price.py` returns 10+ matches
- `grep "missing_cik\|unmapped_cik" scripts/enrich_clusters_with_price.py` returns 4+ matches
- Script imports cleanly: `python -c "import scripts.enrich_clusters_with_price"`
</success_criteria>

<output>
After completion, create `.planning/phases/17-enrichment-pipeline-migration/17-01-SUMMARY.md`
</output>
