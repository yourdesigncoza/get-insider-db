---
phase: 06-production-integration-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/async_client/retry.py
  - src/async_client/http_client.py
  - src/async_client/db_engine.py
autonomous: true

must_haves:
  truths:
    - "Async modules use structlog, not stdlib logging"
    - "Retry events logged with structured context (ticker, attempt, wait_time)"
    - "HTTP client logs connection events at debug level"
  artifacts:
    - path: "src/async_client/retry.py"
      provides: "Structured logging for retry events"
      contains: "from src.logging_config import get_logger"
    - path: "src/async_client/http_client.py"
      provides: "Debug logging for HTTP operations"
      contains: "from src.logging_config import get_logger"
    - path: "src/async_client/db_engine.py"
      provides: "Debug logging for engine creation"
      contains: "from src.logging_config import get_logger"
  key_links:
    - from: "src/async_client/*.py"
      to: "src/logging_config.py"
      via: "get_logger import"
      pattern: "from src.logging_config import get_logger"
---

<objective>
Replace stdlib logging with structlog in async_client modules

Purpose: Phase 02 established structlog as the standard (get_logger pattern), but async_client modules created in Phase 03 use stdlib logging. This creates inconsistent log output — some structured, some not. Standardize all async modules to use structlog.

Output: All async_client modules use get_logger() from src.logging_config.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/logging_config.py
@src/async_client/retry.py
@src/async_client/http_client.py
@src/async_client/db_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update retry.py to use structlog</name>
  <files>src/async_client/retry.py</files>
  <action>
Replace stdlib logging with structlog in retry.py:

1. Remove: `import logging`
2. Remove: `logger = logging.getLogger(__name__)`
3. Add: `from src.logging_config import get_logger`
4. Add: `logger = get_logger(__name__)`

5. The tenacity `before_sleep_log` callback requires a stdlib logger. Create a custom before_sleep callback that uses structlog instead:

```python
def _before_sleep_structlog(retry_state):
    """Log retry attempts with structlog."""
    logger.warning(
        "retry_attempt",
        attempt=retry_state.attempt_number,
        wait_seconds=round(retry_state.next_action.sleep, 2),
        exception=str(retry_state.outcome.exception()) if retry_state.outcome else None,
    )
```

6. Replace `before_sleep=before_sleep_log(logger, logging.WARNING)` with `before_sleep=_before_sleep_structlog`

7. Remove the now-unused `before_sleep_log` import from tenacity imports.
  </action>
  <verify>python -c "from src.async_client.retry import async_retry; print('import ok')" && ! grep -q "import logging" src/async_client/retry.py</verify>
  <done>retry.py uses structlog, no stdlib logging imports</done>
</task>

<task type="auto">
  <name>Task 2: Add structlog to http_client.py</name>
  <files>src/async_client/http_client.py</files>
  <action>
Add structured debug logging to http_client.py:

1. Add import: `from src.logging_config import get_logger`
2. Add: `logger = get_logger(__name__)`

3. Add debug logging in key operations:
   - In `_get_session()`: `logger.debug("http_session_created")` when creating new session
   - In `get()`: `logger.debug("http_request", method="GET", url=full_url)` before request
   - In `close()`: `logger.debug("http_session_closed")` after closing

Keep logging minimal (debug level only) to avoid noise. HTTP operations are high-frequency.
  </action>
  <verify>python -c "from src.async_client.http_client import AsyncHTTPClient; print('import ok')" && grep -q "get_logger" src/async_client/http_client.py</verify>
  <done>http_client.py has structured debug logging</done>
</task>

<task type="auto">
  <name>Task 3: Add structlog to db_engine.py</name>
  <files>src/async_client/db_engine.py</files>
  <action>
Add structured debug logging to db_engine.py:

1. Add import: `from src.logging_config import get_logger`
2. Add: `logger = get_logger(__name__)`

3. Add debug logging:
   - In `get_async_engine()`: `logger.debug("async_engine_created", pool_size=pool_size, max_overflow=max_overflow)` when engine is created (inside the function, after create_async_engine call but before return)
   - In `dispose_engine()`: `logger.debug("async_engine_disposed")` after dispose

Note: get_async_engine is cached with @lru_cache, so the log will only appear on first call.
  </action>
  <verify>python -c "from src.async_client.db_engine import get_async_engine; print('import ok')" && grep -q "get_logger" src/async_client/db_engine.py</verify>
  <done>db_engine.py has structured debug logging</done>
</task>

</tasks>

<verification>
- No `import logging` in any async_client/*.py file (except if needed for type hints)
- All three files have `from src.logging_config import get_logger`
- pytest tests/ passes (no regressions)
- Manual: Run with LOG_LEVEL=DEBUG to see structured output
</verification>

<success_criteria>
- All async_client modules use structlog via get_logger()
- Retry attempts logged with structured context (attempt, wait_seconds, exception)
- HTTP and DB operations have debug-level logging
- No stdlib logging in async_client modules
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-integration-cleanup/06-02-SUMMARY.md`
</output>
