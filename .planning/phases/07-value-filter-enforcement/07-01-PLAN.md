---
phase: 07-value-filter-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scoring_config/scoring_weights.py
  - src/analytics/cluster_buys.py
  - tests/test_cluster_scoring.py
autonomous: true

must_haves:
  truths:
    - "find_cluster_buys() defaults to 500K min_total_value and 50K min_trade_value from ClusterThresholds"
    - "find_tradeable_cluster_signals() defaults to 500K min_total_value and 50K min_trade_value from ClusterThresholds"
    - "ClusterScoringWeights.w_value is 3.0 (increased from 2.0)"
    - "ClusterThresholds.min_trade_value_usd is 50_000.0 (increased from 0.0)"
    - "All existing tests pass with updated assertions"
  artifacts:
    - path: "src/scoring_config/scoring_weights.py"
      provides: "Updated ClusterThresholds and ClusterScoringWeights"
      contains: "min_trade_value_usd: float = 50_000.0"
    - path: "src/analytics/cluster_buys.py"
      provides: "Config-wired function signatures"
      contains: "CLUSTER_THRESHOLDS.min_total_value_usd"
  key_links:
    - from: "src/analytics/cluster_buys.py"
      to: "src/scoring_config/scoring_weights.py"
      via: "import CLUSTER_THRESHOLDS"
      pattern: "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS"
---

<objective>
Wire ClusterThresholds config defaults into core detection functions and update scoring weight.

Purpose: Eliminate the structural disconnect where ClusterThresholds defines sensible defaults (500K total, now 50K per-trade) but core functions default to 0.0, bypassing the config entirely.

Output: Core functions use config values as defaults; scoring weight increased; tests updated.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/scoring_config/scoring_weights.py
@src/analytics/cluster_buys.py
@src/cluster_scoring.py
@tests/test_cluster_scoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config defaults and scoring weight</name>
  <files>
    src/scoring_config/scoring_weights.py
    tests/test_cluster_scoring.py
  </files>
  <action>
    In `src/scoring_config/scoring_weights.py`:
    1. Change `ClusterThresholds.min_trade_value_usd` from `0.0` to `50_000.0`
    2. Change `ClusterScoringWeights.w_value` from `2.0` to `3.0`

    In `tests/test_cluster_scoring.py`:
    The w_value change from 2.0→3.0 shifts Case 1 raw score from ~59.5 to ~65.5, which maps to ~63.6 via saturation curve. Update the assertion range from `58 < score < 62` to `62 < score < 66`. Case 2 stays within 80-90 (shifts from ~82.7 to ~84.1). Case 3 stays at ~100. Update the comment explaining the expected raw score calculation to reflect w_value=3.0.
  </action>
  <verify>
    Run `pytest tests/test_cluster_scoring.py -v` — all tests pass.
    Run `python -c "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS, SCORING_WEIGHTS; assert CLUSTER_THRESHOLDS.min_trade_value_usd == 50_000.0; assert SCORING_WEIGHTS.w_value == 3.0; print('OK')"` — prints OK.
  </verify>
  <done>
    ClusterThresholds.min_trade_value_usd == 50_000.0 and ClusterScoringWeights.w_value == 3.0. Scoring test assertions updated and passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CLUSTER_THRESHOLDS into core detection functions</name>
  <files>
    src/analytics/cluster_buys.py
  </files>
  <action>
    1. Add import at top of file:
       `from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS`

    2. In `find_cluster_buys()` (line 242), change:
       - `min_total_value: float = 0.0` → `min_total_value: float = CLUSTER_THRESHOLDS.min_total_value_usd`
       - `min_trade_value: float = 0.0` → `min_trade_value: float = CLUSTER_THRESHOLDS.min_trade_value_usd`

    3. In `find_tradeable_cluster_signals()` (line 735), change:
       - `min_total_value: float = 0.0` → `min_total_value: float = CLUSTER_THRESHOLDS.min_total_value_usd`
       - `min_trade_value: float = 0.0` → `min_trade_value: float = CLUSTER_THRESHOLDS.min_trade_value_usd`

    Do NOT change any SQL logic — the SQL already applies these parameters correctly. Only the Python function signature defaults change.
  </action>
  <verify>
    Run `pytest tests/ -v` — all tests pass (the tradeable window tests pass min_total_value explicitly, so they are unaffected by default change).
    Run `python -c "import inspect; from src.analytics.cluster_buys import find_cluster_buys; sig = inspect.signature(find_cluster_buys); assert sig.parameters['min_total_value'].default == 500_000.0; assert sig.parameters['min_trade_value'].default == 50_000.0; print('OK')"` — prints OK.
  </verify>
  <done>
    Both find_cluster_buys() and find_tradeable_cluster_signals() default to ClusterThresholds values (500K total, 50K per-trade). Callers who pass explicit values are unaffected.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` — all tests pass
2. `python -c "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS; print(f'total={CLUSTER_THRESHOLDS.min_total_value_usd}, trade={CLUSTER_THRESHOLDS.min_trade_value_usd}')"` — prints `total=500000.0, trade=50000.0`
3. `python -c "import inspect; from src.analytics.cluster_buys import find_cluster_buys, find_tradeable_cluster_signals; s1 = inspect.signature(find_cluster_buys); s2 = inspect.signature(find_tradeable_cluster_signals); assert s1.parameters['min_total_value'].default == 500_000.0; assert s2.parameters['min_total_value'].default == 500_000.0; print('Both wired')"` — prints "Both wired"
</verification>

<success_criteria>
- ClusterThresholds.min_trade_value_usd = 50_000.0
- ClusterScoringWeights.w_value = 3.0
- find_cluster_buys() defaults reference CLUSTER_THRESHOLDS
- find_tradeable_cluster_signals() defaults reference CLUSTER_THRESHOLDS
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-value-filter-enforcement/07-01-SUMMARY.md`
</output>
