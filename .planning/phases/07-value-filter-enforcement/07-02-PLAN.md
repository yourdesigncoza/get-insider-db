---
phase: 07-value-filter-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/show_cluster_buys.py
  - scripts/export_top_clusters.py
  - scripts/backtest_cluster_strategy.py
autonomous: true

must_haves:
  truths:
    - "show_cluster_buys.py --min-total-value defaults to 500K from ClusterThresholds"
    - "show_cluster_buys.py --min-trade-value defaults to 50K from ClusterThresholds"
    - "export_top_clusters.py --min-total-value defaults to 500K from ClusterThresholds"
    - "export_top_clusters.py --min-trade-value defaults to 50K from ClusterThresholds"
    - "backtest_cluster_strategy.py --min-total-value defaults to 500K from ClusterThresholds"
    - "backtest_cluster_strategy.py --min-trade-value defaults to 50K from ClusterThresholds"
    - "Users can still override defaults via CLI flags"
  artifacts:
    - path: "scripts/show_cluster_buys.py"
      provides: "Config-wired CLI defaults"
      contains: "CLUSTER_THRESHOLDS.min_total_value_usd"
    - path: "scripts/export_top_clusters.py"
      provides: "Config-wired CLI defaults"
      contains: "CLUSTER_THRESHOLDS.min_total_value_usd"
    - path: "scripts/backtest_cluster_strategy.py"
      provides: "Config-wired CLI defaults"
      contains: "CLUSTER_THRESHOLDS.min_total_value_usd"
  key_links:
    - from: "scripts/show_cluster_buys.py"
      to: "src/scoring_config/scoring_weights.py"
      via: "import CLUSTER_THRESHOLDS"
      pattern: "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS"
    - from: "scripts/export_top_clusters.py"
      to: "src/scoring_config/scoring_weights.py"
      via: "import CLUSTER_THRESHOLDS"
      pattern: "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS"
    - from: "scripts/backtest_cluster_strategy.py"
      to: "src/scoring_config/scoring_weights.py"
      via: "import CLUSTER_THRESHOLDS"
      pattern: "from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS"
---

<objective>
Wire ClusterThresholds config defaults into all CLI script argument parsers.

Purpose: CLI scripts currently default --min-total-value and --min-trade-value to 0, requiring users to manually pass the intended thresholds. This plan makes the CLI defaults match the config, while still allowing override via flags.

Output: All 3 CLI scripts import and use CLUSTER_THRESHOLDS for value filter defaults.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-value-filter-enforcement/07-01-SUMMARY.md
@src/scoring_config/scoring_weights.py
@scripts/show_cluster_buys.py
@scripts/export_top_clusters.py
@scripts/backtest_cluster_strategy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CLUSTER_THRESHOLDS into CLI script defaults</name>
  <files>
    scripts/show_cluster_buys.py
    scripts/export_top_clusters.py
    scripts/backtest_cluster_strategy.py
  </files>
  <action>
    For each of the 3 scripts:

    1. Add import at top:
       `from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS`

    2. Change `--min-total-value` default from `0` (or `0.0`) to `CLUSTER_THRESHOLDS.min_total_value_usd`

    3. Change `--min-trade-value` default from `0` (or `0.0`) to `CLUSTER_THRESHOLDS.min_trade_value_usd`

    4. Update help text to indicate the default source, e.g.:
       `help="Minimum total cluster value (default: from config)"`
       `help="Minimum per-trade value (default: from config)"`

    **Specific locations:**
    - `show_cluster_buys.py`: lines 147-148 (argparse add_argument calls)
    - `export_top_clusters.py`: lines 87-88 (argparse add_argument calls)
    - `backtest_cluster_strategy.py`: lines 202-203 (argparse add_argument calls)

    Users can still pass `--min-total-value 0` to override and get all clusters.
  </action>
  <verify>
    For each script, verify the default is wired:
    ```bash
    python -c "
    import sys; sys.argv = ['test']
    from scripts.show_cluster_buys import main
    # Just verify import works
    from src.scoring_config.scoring_weights import CLUSTER_THRESHOLDS
    print(f'show: thresholds loaded, total={CLUSTER_THRESHOLDS.min_total_value_usd}, trade={CLUSTER_THRESHOLDS.min_trade_value_usd}')
    "
    ```

    Grep all 3 scripts for `CLUSTER_THRESHOLDS` import — should find it in all 3.
    Grep all 3 scripts for `default=0` on min-total-value/min-trade-value — should find zero matches.
  </verify>
  <done>
    All 3 CLI scripts default --min-total-value to 500K and --min-trade-value to 50K via CLUSTER_THRESHOLDS import. No hardcoded 0 defaults remain for value filters.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "CLUSTER_THRESHOLDS" scripts/show_cluster_buys.py scripts/export_top_clusters.py scripts/backtest_cluster_strategy.py` — import and usage present in all 3
2. `grep -n "default=0" scripts/show_cluster_buys.py scripts/export_top_clusters.py scripts/backtest_cluster_strategy.py | grep -i "value"` — no matches (no hardcoded 0 for value filters)
3. `pytest tests/ -v` — all tests pass (no test regression)
</verification>

<success_criteria>
- All 3 CLI scripts import CLUSTER_THRESHOLDS
- --min-total-value defaults to CLUSTER_THRESHOLDS.min_total_value_usd in all scripts
- --min-trade-value defaults to CLUSTER_THRESHOLDS.min_trade_value_usd in all scripts
- No hardcoded 0 defaults remain for value filter arguments
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-value-filter-enforcement/07-02-SUMMARY.md`
</output>
