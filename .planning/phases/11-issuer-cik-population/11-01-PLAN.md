---
phase: 11-issuer-cik-population
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [schema.sql, tests/test_issuer_cik_population.py]
autonomous: true

must_haves:
  truths:
    - "scan_clusters.py output contains non-null issuer_cik for every row"
    - "issuer_cik values are SEC-standard CIK identifiers from form345_submission"
    - "Existing Python code picks up issuer_cik automatically via _get_optional_column()"
  artifacts:
    - path: "schema.sql"
      provides: "insider_buy_signals view with issuer_cik column"
      contains: 's."ISSUERCIK" AS issuer_cik'
    - path: "tests/test_issuer_cik_population.py"
      provides: "Unit tests for CIK population in cluster output"
      min_lines: 20
  key_links:
    - from: "schema.sql (insider_buy_signals view)"
      to: "src/analytics/cluster_buys.py line 413"
      via: "_get_optional_column inspects view columns at runtime"
      pattern: '_get_optional_column.*"issuer_cik"'
    - from: "src/analytics/cluster_buys.py line 595"
      to: "JSON output line 700"
      via: "_first_nonempty_any extracts CIK from subset, passes to output dict"
      pattern: 'issuer_cik.*_first_nonempty_any'
---

<objective>
Add `issuer_cik` column to the `insider_buy_signals` SQL view so cluster scan output populates the field automatically.

Purpose: Clusters currently export `issuer_cik: null` because the view omits the `ISSUERCIK` column from `form345_submission`. The Python code already handles the column defensively via `_get_optional_column()` -- once the view exposes it, everything works end-to-end with zero Python changes.

Output: Updated schema.sql with view column, test file validating CIK population logic.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@schema.sql (lines 265-290 — insider_buy_signals view)
@src/analytics/cluster_buys.py (lines 57-68 — _get_optional_column; lines 413-433 — CIK fetch; line 595 — CIK extraction; line 700 — output assembly)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add issuer_cik to insider_buy_signals view and apply migration</name>
  <files>schema.sql</files>
  <action>
In `schema.sql`, modify the `insider_buy_signals` view definition (line 265-287) to add `s."ISSUERCIK" AS issuer_cik` between the `issuer_name` line and the `insider_cik` line. The new column must:
- Use double-quoted uppercase source column: `s."ISSUERCIK"` (PostgreSQL case sensitivity)
- Alias to lowercase: `AS issuer_cik`
- Be placed after `s."ISSUERNAME" AS issuer_name,` and before `r."RPTOWNERCIK" AS insider_cik,`

After editing schema.sql, apply the migration to the running database:
```bash
psql $DATABASE_URL -c "DROP VIEW IF EXISTS public.insider_buy_signals;"
psql $DATABASE_URL -c "$(grep -A 25 'CREATE VIEW public.insider_buy_signals' schema.sql)"
psql $DATABASE_URL -c "ALTER VIEW public.insider_buy_signals OWNER TO myuser;"
```

Do NOT create a separate migration file. The schema.sql IS the canonical DDL.

Verify the column exists after migration:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'insider_buy_signals' AND column_name = 'issuer_cik';
```
  </action>
  <verify>
Run: `psql $DATABASE_URL -c "SELECT issuer_cik FROM insider_buy_signals LIMIT 5;"` — should return non-null CIK values (10-digit zero-padded strings like "0000730255").
  </verify>
  <done>
insider_buy_signals view contains issuer_cik column; column returns actual CIK values from form345_submission.ISSUERCIK; schema.sql matches live database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for issuer_cik population</name>
  <files>tests/test_issuer_cik_population.py</files>
  <action>
Create `tests/test_issuer_cik_population.py` following the pattern in `tests/test_fund_ratio_filtering.py` (pure unit tests, no DB dependency).

Tests to write:

1. **test_issuer_cik_included_in_output_dict** — Build a mock output dict matching the structure at cluster_buys.py line 697-714. Assert `issuer_cik` key exists and is not None.

2. **test_issuer_cik_extraction_from_subset** — Replicate the extraction logic from line 595: given a DataFrame with an `issuer_cik` column containing mixed values (some filled, some empty), verify `_first_nonempty_any()` returns the first non-empty value. Import `_first_nonempty_any` from `src.analytics.cluster_buys`.

3. **test_issuer_cik_missing_column_fallback** — Replicate the fallback at line 595: when `issuer_cik` is NOT in subset columns, result should be empty string `""`.

4. **test_issuer_cik_preserves_zero_padding** — Verify CIK strings like "0000730255" are not truncated to "730255". This guards against accidental int casting (Pitfall 1 from research).

5. **test_get_optional_column_finds_issuer_cik** — Mock `inspect(engine).get_columns()` to return a column list including `issuer_cik`. Verify `_get_optional_column(engine, "insider_buy_signals", ("issuer_cik", "cik"))` returns `"issuer_cik"`. Import `_get_optional_column` from `src.analytics.cluster_buys`.

Keep tests database-free — use pandas DataFrames and mocks only.
  </action>
  <verify>
Run: `pytest tests/test_issuer_cik_population.py -v` — all tests pass.
  </verify>
  <done>
5 tests covering: output dict structure, extraction logic, missing column fallback, zero-padding preservation, and column introspection. All pass.
  </done>
</task>

</tasks>

<verification>
1. `psql $DATABASE_URL -c "SELECT issuer_cik FROM insider_buy_signals LIMIT 3;"` returns non-null CIK values
2. `pytest tests/test_issuer_cik_population.py -v` — all 5 tests pass
3. `python scripts/scan_clusters.py --limit 5 --print` — output shows populated issuer_cik values (not null/empty)
4. `pytest tests/ -v` — full test suite passes (no regressions)
</verification>

<success_criteria>
- insider_buy_signals view includes issuer_cik column (schema.sql updated)
- Live database view returns CIK values from form345_submission.ISSUERCIK
- Zero Python code changes needed (existing _get_optional_column handles it)
- 5 regression tests pass covering CIK extraction, fallback, and format preservation
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-issuer-cik-population/11-01-SUMMARY.md`
</output>
